import React from "react";

const Projects = () => {
  return (
    <div>
      Projects Free React Admin Template CoreUI Free Admin Panel is not another
      blend of 3rd parties free components and libraries. It's the only Open
      Source React Admin Dashboard Template built on the enterprise-grade
      hand-crafted UI Components Library created and backed by professionals.
      CoreUI helps you build reliable web apps faster than before.
    </div>
  );
};

export default Projects;
